import React from 'react'

const Avatar = (props) => {
  return (
    <div>
        <h1>{props.user}</h1>
        <h1>{props.user}</h1>
        <p>{props.avatarSize}</p>
    </div>
  )
}

export default Avatar